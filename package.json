{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "administrator:dev": "cross-env variant=administrator vite --host",
    "administrator:build": "cross-env variant=administrator tsc && vite build",
    "consumer:dev": "cross-env variant=consumer vite --host",
    "consumer:build": "cross-env variant=consumer tsc && vite build",
    "merchant:dev": "cross-env variant=merchant vite --host",
    "merchant:build": "cross-env variant=merchant tsc && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@types/node": "16",
    "@vitejs/plugin-react-swc": "^3.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.2.0",
    "cross-env": "^7.0.3"
  }
}
